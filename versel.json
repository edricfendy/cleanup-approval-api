{
  "version": 2,
  "builds": [
    { "src": "api/approve-cleanup.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/approve-cleanup", "dest": "api/approve-cleanup.js" }
  ]
}
